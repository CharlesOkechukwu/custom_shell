#ifndef _HEADER_
#define _HEADER_

#include <stdio.h>
#include <unistd.h>
#include <string.h>

int _putchar(char c);
int _strlen(char *s);
void _puts(char *s);
char *_strcpy(char *dest, const char *src);

#endif
